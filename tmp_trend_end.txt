                        position="top"
                        offset={10}
                        style={{
                          fontSize: 12,
                          fontWeight: 700,
                          fill: "#10b981",
                        }}
                        formatter={(value: any) =>
                          value
                            ? Math.round(Number(value)).toLocaleString()
                            : "0"
                        }
                      />
                    </Area>
                  )}

                  {historyChartType === "idle" && (
                    <Area
                      type="monotone"
                      dataKey="idle"
                      name="Idle Stock (Kg)"
                      stroke="#f59e0b"
                      strokeWidth={1.5}
                      fillOpacity={1}
                      fill="url(#historyColorIdle)"
                      dot={{
                        r: 4,
                        strokeWidth: 2,
                        stroke: "#f59e0b",
                        fill: "#ffffff",
                      }}
